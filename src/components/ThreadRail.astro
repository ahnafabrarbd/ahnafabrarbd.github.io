---
// ThreadRail — G4 static placeholder: the hairline track at the right gutter
// (≥768px). Scroll-progress pink fill, section labels and scrollspy are G6
// motion work (flag-gated). Layout guarantees the no-crossing rule: content
// max-width stops at the gutter corridor (see .wrap in base.css).
---
<div class="thread-rail" aria-hidden="true"></div>
