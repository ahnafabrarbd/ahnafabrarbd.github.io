# DECISIONS — log of record (with revert paths)

- **D-001** (2026-06-10, G0): Local git checkpoints only, no remotes/pushes — campaign §9 HARD rail overrides the home-directory CLAUDE.md "always push" rule for this project. Revert: owner may add a remote manually after campaign end.
- **D-002** (2026-06-10, G0): Classic theme over block theme — locked by §15, not relitigated.
- **D-003** (2026-06-10, G0): `.env` absent → recon proceeds by public crawl only (§20 fallback). Revert: if owner supplies `.env` in a later session, run the cPanel read-only ladder to enrich reference/server/ before G4.
- **D-004** (2026-06-10, G2): Drive imagery audit found zero hero-grade editorial photography (best assets: 434×651 white-flat product shots, 843×1264 catalogue model shots, one 5464×3640 hazy aerial). Therefore: hero weave composes typography + thread + cutout panels at resolution-honest sizes (no full-bleed photographic hero); the aerial is the sole full-bleed breather candidate (Overview), colour-graded toward the dark ground, caption below per no-text-on-imagery rail. Revert: if owner supplies high-res photography, slots swap via image-map without template changes — that is the point of the content system.
- **D-005** (2026-06-10, G2): Several Drive model shots carry AI-generation watermarks and third-party IP (Peanuts, Sonic, Puma marks) — owner-supplied, so usable as seeds, but flagged in RISKS (R-008) and listed for owner sign-off in FINAL_REPORT. Cutout pipeline prioritises the 28 garment-on-white flats (no people, no visible third-party characters where avoidable).
- **D-006** (2026-06-10, G3): **Form plugin = Contact Form 7** (10M+ installs, GPL-free, shortcode-native `do_shortcode('[contact-form-7 …]')`, jQuery-free since 5.4, hand-authored form markup means the theme owns every tag, thin stable CSS hooks, reCAPTCHA optional). Runner-up Fluent Forms (modern, vanilla JS) if a visual builder is ever wanted. WPForms Lite/Forminator rejected (opinionated generated markup/CSS); Gravity disqualified (paid). Full comparison: reference/design-research.json. Revert: swap shortcode + restyle ~6 CSS hooks.
- **D-007** (2026-06-10, G3): Nav primary set confirmed as the brief's default six — Overview · Capabilities · Products · Sustainability · Partners · Contact — plus the "Menu" trigger (word, not hamburger). Rationale: matches buyer tasks (who are you / what can you make / what do you make / are you compliant / who do you work with / talk to us); Home lives on the wordmark; Career+Gallery in overlay & footer. SOFT — revisit only with critic evidence.
- **D-008** (2026-06-10, G2): Live-site photography audit upgrades D-004: company-profile.jpg (1920×1280 sewing-line close-up) is genuinely editorial-grade; overview.jpg, 15-1-min.jpg (1920px floor shots) and img_0976-scaled.jpg (2560px showroom) anchor chapter/breather imagery. Hero remains type+thread+cutout built, but full-bleed breathers now have real photographic candidates.
- **D-009** (2026-06-10, G3): **Type system = Clash Display / Switzer / Martian Mono** — P2 won the three-judge fresh-context bake-off 2–1 (display authority 9/9/7, expensive-feel 9/9/7); all three judges independently recommended the Martian Mono utility graft (engineering slashed zero, display-matched digit weight, kills the template-familiar Clash+JetBrains combo). Outward Block eliminated unanimously (counter collapse at scale — "do not carry forward in any role"). Watch-item: hand-kern Clash L–T pairs at hero scale (judge 3). Revert path: P3 (Apfel Satt/General Sans/Necto Mono) scored second, fully licensed and downloaded.
- **D-010** (2026-06-10, V7 boot): **CLAUDE-CODE-PROMPT-V7.md is the executed order** — owner-locked Astro pivot. Architecture: from-scratch **Astro 5.x `output:'static'`** + hand-written CSS on DESIGN.md tokens + GSAP (npm, per-page), zero client-JS default, no CSS frameworks, no React/Vue/Svelte by default. WordPress is retired: **D-002 (classic theme) is VOID**; V4–V6 remain as inheritance sources only (RAILS.md). Revert: none — owner Decision Register §3, do not re-open.
- **D-011** (2026-06-10, V7 boot, tag `V6-CONFLICT`): **Zafir's five rules re-targeted.** Rules 1–2 ("It's a WordPress website" / "Build a WordPress theme") conflict with V7 §3 → V7 wins; surviving intent: a complete handable artifact Zafir can deploy and maintain (→ `dist/` + `rizvi-site.zip` + HANDOFF.md). Rule 3 (PLUGINS.md) → void mechanic; intent: every third-party dependency/service documented for manual setup (→ HANDOFF.md + GO-LIVE.md). Rule 4 (don't break Yoast SEO/tracking) → Yoast dies with WordPress; intent: SEO parity (→ V7 §10: per-page meta, sitemap at both /sitemap.xml and /sitemap_index.xml, slugs byte-exact). Rule 5 (README install instructions) → intent: GO-LIVE.md owner checklist + HANDOFF.md. Verbatim originals preserved in RAILS.md.
- **D-012** (2026-06-10, V7 boot, tag `V6-CONFLICT`): **Analytics dropped.** V5-era rule "soft navigations must re-fire the MonsterInsights GA4 pageview (G-J1B3RTG2HV)" conflicts with V7 §3 "No analytics, no trackers, no cookies unless the owner asks in writing" → V7 wins: the new site ships with zero analytics. Owner may request GA4 in writing post-launch (a one-line script add, documented in HANDOFF.md as a flagged option).
- **D-013** (2026-06-10, V7 boot): **cPanel excommunicated (V7 Law 3).** `setup/server-mirror.py` deleted; no allow rule will ever be added; reference = public GET only. **D-003's revert path ("if owner supplies .env, run the read-only ladder") is VOID.** The Session-1 "needs owner: add an allow rule" request is withdrawn. `.env`/`.env.save` remain owner property — never read; owner reminded each session to delete them.
- **D-014** (2026-06-10, V7 boot, tag `V6-CONFLICT`): **D-006 (Contact Form 7) is VOID** — CF7 is a WordPress plugin and dies with WordPress. V7 §9 reopens the form decision with a fixed candidate set: (a) tiny self-hosted PHP handler (server runs PHP 8.0.30), (b) Web3Forms, (c) Formspree free tier. Decision lands at G5 contact-page build with written justification + local test evidence; constraints: free, spam-protected w/o hostile CAPTCHA, accessible, works JS-disabled, no account creation by me.
- **D-015** (2026-06-10, V7 boot, tag `V6-CONFLICT`): **Four further conflicts surfaced by the RAILS.md extraction — V7 wins each; logged for owner review, never silently resolved.** (1) *Budgets:* V4–V6 "JS ≤180KB gz / Lighthouse ≥90" → superseded by V7 §6.1 (JS ≤90KB, LH ≥95 ×4, stricter everywhere). (2) *Packaging docs:* V4–V6 DEPLOY.md / README.md / CONTENT-GUIDE.md → folded into the V7 §12 G8 set (GO-LIVE.md owner checklist + HANDOFF.md for Zafir); intents preserved, filenames die. (3) *Form default:* V6 §14.11 "use Netlify Forms syntax as the default handler" → void (requires Netlify hosting + account; violates V7 §5.1 no-accounts); V7 §9 candidate set stands (D-014). (4) *Motion stack:* V4/V5 "Lenis smooth scroll, barba.js or View Transitions API… minimal WebGL layer (OGL or three.js)" → barba.js/OGL/three.js DEAD (V7 §3: GSAP is the motion library; §6 budgets); Lenis survives ONLY as the DESIGN.md flag-gated option, decided at G6 inside the 90KB JS budget with critic approval; Astro view transitions = optional garnish per V7 §6.2. **Related flag for G6:** DESIGN.md's preloader (thread draws the wordmark ≤1.8s) vs V7 §6.2 ban "preloaders/loading screens… unless a critic-approved exception is logged" — the preloader is the brand signature moment, so it goes to the G6/G7 critic panel as a named exception request; if refused, the reduced-motion variant (instant skip) becomes the default for everyone. Law 1 applies: if it costs speed, it dies.
- **D-016** (2026-06-10, G5): **Icon family = Phosphor (thin weight), MIT — the one site-wide family (V7 §8.1).** Six glyphs ship at G5 (recycle/drop/lightning = environmental block; phone/envelope/map-pin = contact), copied from @phosphor-icons/core (dev dep) into src/illustrations/icons/, currentColor → token-driven, wired through manifest.ts (one-line swaps, §8.3). **Character-illustration sets (unDraw, Open Peeps, Open Doodles, Humaaans, Lukasz Adam) evaluated with licences verified at source and REJECTED on brand fit** — the severe editorial-industrial voice carries imagery photographically; character vectors would be §4 decoration-slop. All licences recorded in CREDITS.md triage table. Revert: any set is licence-clean (CC0/custom-open) — re-admit via manifest.ts if the owner wants illustrative warmth.
- **D-017** (2026-06-10, G5): **Contact form engine = self-hosted `contact.php`** (V7 §9 option a; closes D-014). Justification against the hard constraints: it is the ONLY candidate needing **zero owner pre-launch account work** (Web3Forms requires an access-key signup, Formspree an account + 50/mo cap — both became owner-TODO-blocked); free forever; data never leaves the owner's server (privacy note in the deck copy is honoured literally); plain-POST = works JS-disabled natively; zero client JS; host capability confirmed (PHP 8.0.30 in live headers, RECON). Spam defence without hostile CAPTCHAs: honeypot field + JS-progressive time-trap (enforced only when JS stamped the field, so no-JS humans are never blocked) + per-IP 60s rate limit; header-injection guarded; silent-drop responses never tip off bots. Graceful failure → visible `mailto:` fallback on the page AND in every error response. **Local test = mocked-endpoint contract test** (php binary absent on the build machine; installing system PHP would breach the project-local-installs rail) — evidence in REPORTS/form-mock-test.txt; **live verification is an owner step in GO-LIVE.md** (send a real message, confirm receipt at info@rizvifashions.com). Revert: Web3Forms (option b) is a drop-in action-URL swap + owner key signup, documented in HANDOFF.md.
- **D-018** (2026-06-10, G5, tag `LAW-1-CUT`): **Astro prefetch switched OFF; image-LCP pages redesigned text-first.** The §6.1 LCP ≤2.0s budget proved unreachable for any page whose largest first-viewport element is a half-viewport photograph: simulated slow-4G pins such pages at a ~2.3–2.6s floor that survived seven byte/priority/preload interventions (each logged in PROGRESS with numbers). Resolution per Law 1 (speed wins, cut logged): (a) Overview leads with the Who-We-Are TEXT chapter (the wireframe's "2 text chapters, floor photo" permits the photo second); (b) Gallery's title screen holds the fold alone, plates begin below — editorial pacing consistent with the severe voice; (c) the 2KB prefetch head-module (a §6.2 nicety) is cut from the first-paint graph. Result: **all 11 pages Lighthouse performance 100, LCP green**. Revisit at G6: prefetch may return as a flagged option if budgets hold; the hero/products pages keep pure-text LCP by design.
- **D-019** (2026-06-11, G7): **Showpiece = "The Unbroken Thread"** — 3-judge Fable panel (REVIEWS/G7-showpiece-panel.md), 2 of 3 independent convergence. The pink thread rail becomes session-persistent: instead of dying in a white flash on every navigation, it animates as ONE continuous element across page loads (cross-document View Transitions: `@view-transition{navigation:auto}` + `view-transition-name` on the rail — pure CSS, **0KB JS**, graceful degradation to normal nav where unsupported), with a stitch-tick hand-off mark (existing `--seam-w` vocabulary) at the continuity seam. Makes the campaign's own name structurally true. Feature-flag `unbrokenThread`, reduced-motion = instant cut (tick still renders). Runner-up held: Judge A's "The Turn" (rail bends into the products warp) — strong but page-local; Chanel rule = one showpiece. Revert: flag off → current per-page rail.
- **D-020** (2026-06-11, G7): **Preloader REFUSED** (unanimous 3–0, closes D-015's open exception). The thread-draws-wordmark preloader fails Law 1 — it delays the hero on the first visit against a 2.0s LCP build-failing budget; session-caching concedes it isn't worth showing twice. Per D-015's pre-written outcome, instant-skip is now the default for everyone (flag `preloader` stays false). The brand signature survives: the wordmark-draw beat folds into the hero at t=0, concurrent with content render, zero speed cost. The G7 wordmark trace (public/wordmark.svg, path length ~6743px) feeds that hero beat instead of a preloader.
- **D-021** (2026-06-11, v1.1, owner-requested): **Products = pinned horizontal scroll gallery — BUILT** (the DESIGN.md scroll-grammar "departure" descoped at G6, now delivered; the Breakthrough Energy scroll-driven inspiration the owner asked after). Flag `productsPin`. Desktop + motion: GSAP ScrollTrigger pins the products stage and scrubs the whole cutout track horizontally as the user scrolls vertically; the category index lights the active category. Fallback (mobile / reduced-motion / JS-off): the page's DEFAULT vertical category list — each category a horizontal native-scroll rail (the spec's reduced-motion alternative). DOM stays in logical reading order under the pin (RAILS §1.11); HARD category anchors (#men…#new-born) preserved + JS scrolls the window to the right horizontal position on click/hash. GSAP loads lazily (products JS budget 2KB at load). **Verified:** 60fps median 16.7ms / 0 long tasks at 4× throttle; perf 100×4×11 held (note: required dropping `text-wrap:balance` from headings — it cost the home hero ~50ms LCP); a11y 100 (no `role="group"` on the rail `<ul>`); cross-engine clean; no-JS renders the full list. Revert: flag off → the plain vertical list.
- **D-022** (2026-06-11, v1.2, owner-requested): **Site-wide scroll-driven motion — BUILT, with an honest engineering pullback.** Owner asked for the pinned/crossfade scroll experience on EVERY page (not just Products). What shipped (flag `pinAllPages`, GSAP, desktop + motion only): every chapter's opacity + lift are **coupled to scroll** (scrubbed entrance) so chapters rise and brighten as you scroll into them, on top of the existing thread-rail fill + scrollspy + chapter-seam stitches — every page now reads as a cinematic scroll-driven sequence. **Two literal interpretations were prototyped and REJECTED on quality** (exactly the downsides flagged when the owner chose the maximalist option): (a) *dim-every-non-centred-chapter crossfade* — dimmed content while it was being READ and left the first/last chapters dim at the page extremes (unreadable); (b) *pinning each chapter* — clips content taller than the viewport and makes pages 2–3× longer to scroll. The delivered scrubbed-entrance model is content-safe: **every chapter verifiably reaches full opacity when read** (tested across all pages), nothing clips, pages keep their length. The one true *pin* is Products (horizontal, D-021). Fallback (mobile / reduced-motion / no-JS): the document scroll + lighter G6 reveals (`.pinmode` is desktop-only, set before paint). **Verified:** 60fps median 16.7ms / 0 long tasks @4× throttle; perf 100×4×11 (the gate is mobile, where pinmode is off; GSAP idle-loads on desktop within the 90KB JS budget); cross-engine clean; reduced-motion + no-JS complete. Revert: flag off → the G6 reveals. **Open offer:** a bespoke *literal* pinned-scrollytelling sequence on a single marquee page (Home or Overview), where content can be authored to fit pinned stages without the clipping/length costs — not applied blindly to all 10 content pages.
- **D-023** (2026-06-11, v1.3, owner-requested): **Home story stage — BUILT.** The literal pinned-scrollytelling dissolve the owner wanted, on the page authored for it. The Mission/Vision/Manufacturing trio (three SHORT beats: numeral + label + heading + one paragraph + CTA) pin as ONE full viewport and crossfade in place as you scroll (flag `storyStage`, GSAP, desktop + motion). Model: a **scroll-driven vertical dissolve** — linear opacity (never two scenes at full; max 50/50 at the brief midpoint) + a vertical travel (departing scene lifts + recedes, arriving rises from below) so heading text is offset, not stacked, during the dissolve; settled scenes are fully clean. Two earlier tunings were rejected on inspection (a wide-plateau curve stacked two full headings; a flat crossfade ghosted text) — the captures are in REPORTS/screens/story-*. Fallback (mobile / reduced-motion / no-JS): three stacked two-column scenes (the page's default markup) — complete + readable. Images resolved on the page (M/V/M no longer use PhotoChapter, now unused-import-free). Deep-links/rail-links to #mission/#vision/#manufacturing scroll to that scene's point in the pin. **Verified:** every scene reaches full opacity (long-settle sweep); worst-case overlap is a clean 50/50, never double-full; 60fps median 16.7ms / 0 long tasks @4×; perf 100×4×11 (home story is desktop-gated; GSAP already on home via the hero weave); cross-engine clean; reduced-motion + no-JS complete. Revert: flag off → the three stacked scenes.
- **D-024** (2026-06-11, v1.4, owner-requested) SUPERSEDES D-021/D-022/D-023: **The whole site is a vertical snap deck.** Owner direction: scroll = carousels, Breakthrough style, every page. Built with native **CSS scroll-snap** (`html.deck { scroll-snap-type: y mandatory }`; each top-level section + the footer = a 100svh snap panel) — **0KB JS, native keyboard + touch, works with JS off**. Nested pages dissolved via `display:contents` so inner units become panels (Home story scenes, Products categories); PhotoChapter keeps its 2-col grid (align-content centres it). Owner also asked: removed the right-side chapter-label index (`.deck .rail-index { display:none }`) and made the pink progress bar **bold + big** (`--deck-bar-w: 0.625rem`, full-height right edge, fills with scroll). The three prior scroll experiments are flag-OFF (`productsPin/pinAllPages/storyStage = false`); their script imports were removed (Astro hoists `<script>` tags at build time, so a `{flag && <script>}` guard had silently dropped core.ts from the bundle — fixed). **Verified:** perf 100×4×11; 0 horizontal overflow 320–1440; every capabilities panel fits 100svh exactly; cross-engine 0 console; no-JS snap works; fidelity 245/0; links 0-fail. Revert: `snapDeck:false` → the document-scroll site returns.
