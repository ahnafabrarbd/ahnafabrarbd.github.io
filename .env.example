# Contact form — Web3Forms access key (free: https://web3forms.com).
# Create a key, paste it here as PUBLIC_CONTACT_ACCESS_KEY, and set the same
# value in your CI/deploy env. Without it the form ships a labelled placeholder
# and will NOT deliver messages (the build still succeeds, with a warning).
PUBLIC_CONTACT_ACCESS_KEY=
