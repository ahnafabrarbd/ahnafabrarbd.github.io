# RISKS — production-bite register

| ID | Risk | Severity | Mitigation | Status |
|----|------|----------|------------|--------|
| R-001 | No server-side reference copy (uploads originals, active theme dir). | — | **VOID at V7 boot (D-013): cPanel excommunicated, mirror deleted, no allow rule ever.** All critical recon data was already recovered publicly (PHP floor, counters, anchors, assets). | Closed (V7) |
| R-002 | Animated counter targets may be JS-computed and absent from raw HTML → risk of guessing numbers. | High | RESOLVED at G1: all targets recovered from `data-to-value` attributes. Zero [TBD]. | Closed |
| R-003 | ~~Theme activation conflicts with JupiterX/Elementor~~ → re-targeted: going live by uploading `dist/` into `public_html` over a WordPress install — wrong upload order or missed backup loses rollback. | High | GO-LIVE.md (G8): owner takes the full cPanel backup FIRST, moves WordPress files to `_old-wordpress/` (nothing deleted), uploads the zip; rollback = move files back. Additive `.htaccess` never instructs deleting server rules (V7 §12 G7). | Open (re-targeted) |
| R-004 | Drive folder fetch may be partial (tool caps). | Medium | Closed at G2: 54/54 recovered (3 on retry). | Closed |
| R-005 | SEO regression on slug/anchor drift. | High | Slugs/anchors are now BUILD ASSERTIONS (pages throw if an anchor is missing) + fidelity/link gates + sitemap at both /sitemap-index.xml and /sitemap_index.xml. | Mitigated (gated) |
| R-006 | Brief asserted Yoast installed; production HTML shows no Yoast output. | Low | Moot under V7: the Astro build owns titles/meta/canonical/OG itself (RAILS §2 row 3); parity is to the intended state (V7 §10). | Closed (V7) |
| R-007 | GA4 tracking continuity on soft navigations. | — | **VOID per D-012: V7 bans analytics outright.** Owner may request GA4 in writing post-launch (documented one-line add in HANDOFF.md). | Closed (V7) |
| R-008 | Drive model imagery shows AI-generation watermarks and third-party IP (Peanuts/Snoopy, Sonic/SEGA tees, Puma marks). A sourcing-manager audience may read AI model shots as inauthentic. | Medium | Owner-supplied = usable as seed; shipped slots prioritise garment-on-white flats; every AI/IP-flagged file listed in FINAL_REPORT for owner sign-off; image-map makes replacement one-line. | Open |
| R-009 | All Drive product photography is low-resolution (flats 434×651, models 843×1264); no hero-grade photography exists (D-004). | Medium | Resolution-honest sizes tokenised (`--card-h` ≤420px); IMAGES_TODO requests; slot-swap path. | Open |
| R-010 | Astro 5.18.2 carries a moderate advisory (GHSA-j687 define:vars XSS + server-island replay) whose fix is Astro 6 — locked out by the owner's 5.x rail. | Low | Both vectors inapplicable: fully static build, no server islands; `define:vars` BANNED by token-lint (build-failing). Re-check for a 5.x backport at G7 hardening. | Mitigated |
| R-011 | Long vision-heavy subagents stall (stream watchdog, 3 occurrences in Sessions 2–3: critic ×1, logo curator ×1, copy-extractor died at final report). Risk: silent partial work taken as complete. | Medium | Standing practice: verify agent outputs on disk independently (done each time — zero data loss so far); split vision jobs into smaller scopes; structured-output workflows over single long agents. | Open (operational) |
| R-012 | Pepco logo cannot be re-toned tonally (red-on-yellow wordmark collapses); ships as a name-only cell. | Low | needs-manual queue; owner may supply a white/mono Pepco mark; wall renders the name meanwhile (never a redrawn fake). | Open |
